// Authentication token
export const token = "fakeAuthToken";

// Room name
export const roomName = "fakeRoomName";

// Frontends' URL to contact backend
export const controllersApiURL = "ws://localhost:3001/controllers";
export const mainScreenApiURL = "ws://localhost:3001/mainScreen";

// Frontends' URL for the backend to whitelist
export const controllersURL = "http://localhost:3000";
export const mainScreenURL = "";